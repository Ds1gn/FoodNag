<%= form_for(@food_user) do |f| %>
  <% if @food_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food_user.errors.count, "error") %> prohibited this food_user from being saved:</h2>

      <ul>
      <% @food_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :custom_reminder %><br>
    <%= f.date_select :custom_reminder %>
  </div>

  <%= f.hidden_field :food_id, :value => @food.id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
