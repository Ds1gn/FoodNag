<p id="notice"><%= notice %></p>

<h1>Choose Your Items</h1>

<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @food_categories.each do |food_category| %>
      <tr>
        <td><%= food_category.name %></td>
        <td><%= food_category.image %></td>
        <td><%= link_to 'Show', food_category %></td>
        <td><%= link_to 'Edit', edit_food_category_path(food_category) %></td>
        <td><%= link_to 'Destroy', food_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
 -->



<% @food_categories.each do |fc| %>
<%= fc.name %>: 
<% fc.foods.each do |f| %>

<span class="add_food" data-food-id="<%= f.id %>"><%= f.name %></span>
<%#= link_to f.name, purchase_food_path(f), method: :post %>
<% end %>
<br>
<% end %>
</br></br>
<table>
  <thead>
    <tr>
      <th>Purchase List</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @purchases.each do |p| %>
      <tr>
        <td><%= p.food.name || "None" %></td>

      </tr>
    <% end %>
  </tbody>
</table>



<br>

<%= link_to 'New Food category', new_food_category_path %>
