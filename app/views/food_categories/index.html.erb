<h1>Add Items to Fridge</h1>

<body class="open">
    <div class="nav">
      <div class="bs-example">

        <div class="panel panel-default">
            <div class="panel-heading">
                <h4 class="panel-title">
                    <a data-toggle="collapse" data-parent="#accordion" href="#collapseOne">1. What is HTML?</a>
                </h4>
            </div>
            <div id="collapseOne" class="panel-collapse collapse in">
                <div class="panel-body">
                    <p>HTML stands for HyperText Markup Language. HTML is the main markup language for describing the structure of Web pages. <a href="http://www.tutorialrepublic.com/html-tutorial/" target="_blank">Learn more.</a></p>
                </div>
            </div>
        </div>
      </div>
    </div>
</body>

      <!-- <div class="nav-fruit">
      <ul>
        <% @food_categories.each do |fc| %>
          <li>
            <a href="#"><i class="fa fa-reorder"></i><%= fc.name %>:</a>
              <ul>
                <% fc.foods.each do |f| %>
                <li>
                    <span class="add_food" data-food-name="<%= f.name %>" data-food-id="<%= f.id %>"><%= f.name %></span>
                     <% end %>
                </li>
              </ul>
          </li>
          <% end %>
          </li>
      </ul>
    </div> -->

    <div class="main">
      <p class="button">
        open
      </p>
    </div>


<ul class="purchase_list">
  <% @purchases.each do |p| %>
    <li>
      <%= p.food.name || "None" %>
      <%= link_to 'delete', purchase_path(p), method: :delete %>
    </li>
  <% end %>
</ul>
<br>




    <!-- the wrapper -->
  <!--   <nav id="my-menu">
    <ul class="vertical">
    <% @food_categories.each do |fc| %>
      <li><%= fc.name %>:</li>
        <% fc.foods.each do |f| %>
        <ul>
          <li>
            <span class="add_food" data-food-name="<%= f.name %>" data-food-id="<%= f.id %>"><%= f.name %></span>
          </li>
        </ul>
    <% end %>
    <% end %>
    </nav> -->


  <!-- <ul class="purchase_list">

  <br> -->


  <!-- <div id="menu">
    <nav>
      <h2><a href="#"><i class="fa fa-reorder floatRight cursorPointer"></i>All Categories</h2></a>
      <ul>
        <% @food_categories.each do |fc| %>
          <li>
            <a href="#"><i class="fa fa-reorder"></i><%= fc.name %>:</a>
              <ul>
                <% fc.foods.each do |f| %>
                <li>

                      <span class="add_food" data-food-name="<%= f.name %>" data-food-id="<%= f.id %>"><%= f.name %></span>
                       <% end %>
                </li>
              </ul>
          </li>
    <% end %>
          </li>
      </ul>
    </nav>
  </div> -->

<!--   <ul class="recipe-column">
    <% @purchases.each do |r| %>
  	<li><%= link_to r.recipe.recipe_title, r.recipe.recipe_url %>
  	</li>
  	<li>
  	  <%= link_to (image_tag r.recipe.recipe_image), r.recipe.recipe_url %>
  	</li>
  <% end %> -->


  <!--
  <script>
  ready = function(){

  $(document).ready(function() {
        $("#my-menu").mmenu({
           // options
        }, {
           // configuration
           classNames: {
              vertical: "expand"
           }
        });
     });


  $(document).ready(e);
  $(document).on('page:load', ready);
  </script>

   -->
<!--     </main>
  </div>
</div> -->
